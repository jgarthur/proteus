[package]
name = "proteus"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "proteus"
path = "bin/main.rs"

[lib]
path = "src/lib.rs"

[dependencies]
noise = "0.9"
rand = { version = "0.8", features = ["small_rng"] }

[profile.dev]

[profile.release]
# lto = true
